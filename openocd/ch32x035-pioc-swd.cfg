# CH32X035 DirtyJTAG/CMSIS-DAP composite probe, interface 1.
source [find interface/cmsis-dap.cfg]
cmsis-dap backend usb_bulk
cmsis-dap vid_pid 0x1209 0xc0ca
cmsis-dap usb interface 1
transport select swd
# The firmware currently exposes one fixed nominal 1 MHz PIOC profile.
adapter speed 1000
